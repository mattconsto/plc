/*
  Problem 10: Delayed Feedback

  Sums odds/evens individually, outputting the corresponding one each row.
*/

int odd  = 0;
int even = 0;
int step = false;

loop {
  // Toggle between the two states each line, starts with odd.
  if step = !step then
    console.println_int odd  += console.read_int
  else
    console.println_int even += console.read_int
  done;
}
